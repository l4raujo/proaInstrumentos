import bgImagem from '../components/AreaImg.module.css'

function AreaImg (bgImg){
    return(
        <div className={`${bgImagem.container} ${bgFundo}`}>
        </div>
    )
}

export default AreaImg